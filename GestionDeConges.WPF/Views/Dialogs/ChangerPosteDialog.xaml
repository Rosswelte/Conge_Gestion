<Window x:Class="GestionDeConges.WPF.Views.Dialogs.ChangerPosteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Changer de poste"
        Height="320" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgPageBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0"
                   Text="🔄 Changement de poste"
                   FontSize="16" FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}"
                   Margin="0 0 0 4"/>

        <TextBlock Grid.Row="1"
                   x:Name="TxtEmploye"
                   FontSize="13"
                   Foreground="{StaticResource TextMutedBrush}"
                   Margin="0 0 0 16"/>

        <!-- Nouveau poste -->
        <TextBlock Grid.Row="2" Text="Nouveau poste" FontWeight="SemiBold" Margin="0 0 0 4"/>
        <ComboBox Grid.Row="3"
                  x:Name="CboPostes"
                  materialDesign:HintAssist.Hint="Sélectionnez un poste..."
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  DisplayMemberPath="Nom"
                  Margin="0 0 0 16"/>

        <!-- Date de début -->
        <TextBlock Grid.Row="4" Text="Date de début" FontWeight="SemiBold" Margin="0 8 0 4"/>
        <DatePicker Grid.Row="5"
                    x:Name="DpDateDebut"
                    SelectedDate="{x:Static system:DateTime.Today}"
                    Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                    Margin="0 0 0 20"
                    xmlns:system="clr-namespace:System;assembly=mscorlib"/>

        <!-- Boutons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Annuler"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Width="90" Margin="0 0 8 0"
                    Click="BtnAnnuler_Click"/>
            <Button Content="Changer"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="100"
                    Click="BtnChanger_Click"/>
        </StackPanel>
    </Grid>
</Window>