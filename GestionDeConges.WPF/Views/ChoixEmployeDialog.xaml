<Window x:Class="GestionDeConges.WPF.Views.Dialogs.ChoixEmployeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Accès Employé"
        Height="280" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgPageBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="👤 Sélectionnez votre nom"
                   FontSize="16" FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}"
                   Margin="0 0 0 8"/>

        <TextBlock Grid.Row="1"
                   Text="Vous pourrez uniquement soumettre des demandes de congé.&#x0a;Elles seront examinées par un administrateur."
                   FontSize="12"
                   Foreground="{StaticResource TextMutedBrush}"
                   TextWrapping="Wrap"
                   Margin="0 0 0 16"/>

        <ComboBox Grid.Row="2"
                  x:Name="CboEmployes"
                  materialDesign:HintAssist.Hint="Choisissez votre nom..."
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  DisplayMemberPath="NomComplet"
                  Margin="0 0 0 16"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Annuler"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Width="90" Margin="0 0 8 0"
                    Click="BtnAnnuler_Click"/>
            <Button Content="Continuer"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="110"
                    Click="BtnOk_Click"/>
        </StackPanel>
    </Grid>
</Window>
